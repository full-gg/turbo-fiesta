import { HomeButton } from '../HomeButton';
import { MapButton } from '../MapButton';

const Institute = () => {
	return (
		<div className='bank'>
			<HomeButton />
			<MapButton />
		</div>
	);
};

export default Institute;
